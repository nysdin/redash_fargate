name: Deploy To ECS
on:
  workflow_dispatch:
  push:
    - main

jobs:
  ecs-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Configure AWS Credentials
        with: aws-retion: ap-northeast-1
        6938fd4d98bab03faadb97b34396831e3780aea1 DigiCert
