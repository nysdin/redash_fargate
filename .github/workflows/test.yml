name: Test
on:
  workflow_dispatch:
  push:

env:
  date: $(date "+%Y%m%d")
  hoge: hoge

jobs:
  test:
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://syuktasu-kaigi.jp
    steps:
      - run: which tty
      - run: ps aux
      - run: |
          branch=${{ github.ref_name }}
          echo "branch=${branch//\//-}"
