name: Test
on:
  workflow_dispatch:
  # push:

env:
  date: $(date "+%Y%m%d")
  hoge: hoge

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: pwd
      - run: whoami
      - run: echo $GITHUB_WORKSPACE
      - uses: actions/checkout@v3
      - run: pwd
