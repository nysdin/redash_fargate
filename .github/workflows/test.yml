name: Test
on:
  workflow_dispatch:

env:
  date: $(date "+%Y%m%d")
  hoge: hoge

jobs:
  test:
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://syuktasu-kaigi.jp
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: echo
        run: |
          echo ${{ env.date }}
